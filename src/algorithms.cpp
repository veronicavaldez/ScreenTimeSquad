#include "algorithms.h"

void algorithms::printUserDuration(User &user) {
    std::cout << "User ID: " << user.userID << " Duration: " << user.Duration << std::endl;
}

void algorithms::addDuration(User &user, double Duration) {
    // add the duration to the User struct
}

double algorithms::durationCalc(int opened, int closed) {
    // calculate the duration of the user
}

void algorithms::mergeSort() {
    // figure out parameters needed
}

void algorithms::quickSort() {
    // figure out parameters needed
}

void algorithms::Timer() {
    // determine how long each algorithm takes to run
}

